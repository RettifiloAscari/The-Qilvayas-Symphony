# OS / editor cruft
Thumbs.db
desktop.ini
.DS_Store
*.swp
*~

# Google Drive artifacts
*.gdoc
*.gsheet
*.gslides
.tmp.drivedownload/
.tmp.driveupload/

# Build scratch
node_modules/
/export/
scripts/tpl/
scripts/work_tpl/
scripts/work_src/
scripts/_template_decoded.docx

# Generated PDFs are not committed; the .docx in documents/ are, deliberately,
# so the corpus can be opened from any device without a build step.
*.pdf
